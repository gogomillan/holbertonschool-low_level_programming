/**
 * _abs - This function calculate the abs value
 * @n: The value to evaluate
 *
 * Return: 1 if n is greater than zero, 0 is zero and -1 if n is less than zero
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	return (n * -1);
}
